import 'package:flutter/cupertino.dart';

class archivedMobile extends StatelessWidget {
  const archivedMobile({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(child: const Text('arcived'));
  }
}
