import 'package:flutter/cupertino.dart';

class NewMobiles extends StatelessWidget {
  const NewMobiles({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(child: const Text('news'));
  }
}
