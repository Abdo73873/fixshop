import 'package:flutter/cupertino.dart';

class DoneMobiles extends StatelessWidget {
  const DoneMobiles({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(child: const Text('done'));
  }
}
